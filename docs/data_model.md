# Data Model

## Overview
The data model for ArcTracker v2 defines how data is structured and stored in the database.

## Entities
- **User**: Represents an individual using the application.
- **Tracker**: Represents the tracking entities controlled by users.

### Relationships
- A user can have multiple trackers.
- Each tracker is associated with one user.